<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty name="serviceName" source="service.name" defaultValue="unknown"/>

    <!-- JSON to console (single-line per event, tailed by Alloy into Loki).
         Logs intentionally do NOT go over OTLP anymore; only traces do. -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- service.name -> service_name (matches Loki's normalized index label).
                 traceId/spanId (micrometer MDC keys) -> trace_id/span_id so Tempo's
                 "goto logs" trace_id filter matches. -->
            <customFields>{"service_name":"${serviceName}"}</customFields>
            <provider class="net.logstash.logback.composite.loggingevent.MdcJsonProvider">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <mdcKeyFieldName>traceId=trace_id</mdcKeyFieldName>
                <mdcKeyFieldName>spanId=span_id</mdcKeyFieldName>
            </provider>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
</configuration>